# W27-FIX-00 · The fix-pass rulings, recorded first

Card W27-FIX-00. Branch `w27/w27-fix-00-rulings`, from `main` at `383cf8a` (#153 merged).
First card of the wave 27 fix pass, under the same self-merge protocol as the final run.

## What this card does

- **Records your five rulings, word for word**, W27-R-09 to W27-R-13, appended to
  `docs/rulings/W27-R.md` with my reading under each one for you to ratify or overturn.
- **Confirms where the site stands**: `main` at `383cf8a` is live, nothing is open, the branch is
  still unprotected, so each merge waits for the quality check exactly as before.
- **Closes two questions in their headings** (Q-W27-02, Q-W27-03) with the rulings that answer them.

## Two things I found while reading the rulings, and left for you

- **The "unnamed" fourth colour has a name on Imperlux's own pages**: every metal tile page's colour
  picker lists it, RAL 9005 Negru. Your ruling says "+1", so the cards will print "+1"; naming it is
  one word per model whenever you want.
- **Imperlux makes the seven metal tiles itself** (its pages say brand Imperlux, ArcelorMittal steel),
  so there is no other manufacturer to take a packshot from. Imperlux publishes a clean 1080x1080
  render of each model without its orange mark; that is the picture FIX-06 will use for those seven.

## Heights

No page changes. Documents only.

## Gates

Static gates on this tree exit 0: template literals, build, css collisions, links, stale docs, merge
artifacts, stub count, dashes. CI runs the full 29 on the pull request.
