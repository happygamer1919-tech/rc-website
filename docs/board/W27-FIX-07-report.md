# W27-FIX-07 · The fix-pass report

Card W27-FIX-07. Branch `w27/w27-fix-07-report`, from `main` after #158 merged and verified live.

## What this does

Writes `docs/reports/W27-FIX-PASS.md`: what each card did, its pull request, its merge and the live
check on it; the seven tiles kept on Imperlux's own pictures and why; the gate count on the final
tree; Lighthouse on the roofing page in both languages; the deviations; and five lines for you.

Moves the five merged cards' statuses in the backlog to MERGED with the live check's result. That
is the board: one file per card under `docs/board/`, and the statuses in the backlog.

## What to look at

`docs/reports/W27-FIX-PASS.md`, the "For the morning" list at the foot.

## Heights

No page changes. Documents only.

## Gates

Static gates exit 0. CI runs the full 29.
