# W27-FIX-09 · The offer bar on orange again

Card W27-FIX-09. Branch `w27/w27-fix-09-promo-orange`, stacked on W27-FIX-08.

## What this does

Your instruction this morning: "Reducere 10% la orice serviciu doar până în 2027" sits on a full
orange band again, dark text on orange, on every page, right under the header. The design pass had
made it a quiet cream ribbon with an orange dot; that is undone for the bar and nothing else.

Dark text rather than white, on purpose: dark on this orange reads at 5.1:1, which passes the
accessibility floor for small text; white on it would not.

## What to look at

`https://rapidconstruct.md/`: the orange band under the header, on the phone too.

## Heights

None move: the bar is the same height.

## Gates

Static gates and the text-contrast gate exit 0. CI runs the full 29.
