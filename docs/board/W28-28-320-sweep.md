# W28-28 · The 320 sweep

R-W28-13 (the dispatch's "W28-26", an id already taken). A check renders every built page at 320 wide, both locales, and requires the document scrollWidth to equal 320; it becomes gate 35. Every overflow found is fixed here and listed in the report. W28-FIX-02 is one of them.

## Done

- The list: 85 html files, 14 redirects skipped, 71 measured (36 RO, 35 RU). Before the fix one failed, `/ru/` at 337px, from the portfolio filter chip "Проектирование и 3D-визуализация". After the fix, 71 of 71 measure 320.
- The chip may wrap below 360 only; nothing changes at 360 and above.
- Gate 35 registered; `quality` runs 34 commands.
