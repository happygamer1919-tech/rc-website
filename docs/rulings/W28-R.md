# RULING REGISTER W28-R · The wave 28 owner rulings, from 2026-09-23

**This file is the register of the `R-W28` ruling ids.** It continues
`docs/rulings/W27-R.md`, which holds `W27-R-01` to `W27-R-21` and is not renumbered. The ids
here are written the way the owner wrote them, `R-W28-01` onward, with the hyphens.

**Append-only under R-S.** Every ruling below is quoted exactly as the owner gave it in the
wave 28 dispatch of 2026-09-23 (evening, UTC) and its addendum of the same evening. The prose
under each quote is this terminal's reading and is open for ratification; the quote is not.

**The dispatch's own framing, recorded verbatim because it governs every card this wave:**

> EXECUTOR - boot per CLAUDE.md, work the board. Open wave 28 in rc-website. Main at bc3e3a9.
> Rulings first: write docs/rulings/W28-R.md with R-W28-01 (...). Append the cards below to the
> board JSON in docs/board/ with defaults, depends_on, machine-checkable acceptance, then work
> them in order, one card one PR, skip-not-halt, republish the board artifact after every board
> change. Plain hyphens only in commits, notes, code. Credential values never printed.

And from the addendum:

> Self-merge on green stays for the whole wave per R-W28-01, owner confirmed. Run through to
> W28-21 without waiting for owner input; anything blocked goes to the report, not a halt.

**Three premises in that framing were measured false at boot and are corrected here rather
than obeyed** (docs/CLAUDE.md, ground truth is the repo):

1. **`main` was at `5751d2c`, not `bc3e3a9`.** `bc3e3a9` is where the fix pass stood at 07:50
   UTC (#159); eight more cards merged during the day, #160 to #167 (W27-R-14 to W27-R-21,
   `docs/reports/W27-FIX-PASS.md`). Wave 28 opens on `5751d2c` (#167), the tip of `origin/main`
   at 23:08 UTC, with nothing open.
2. **No board JSON existed in `docs/board/`, and no board artifact.** The board of this repo
   has always been `docs/board/<card>.md` per card plus the statuses in `docs/BACKLOG.md`
   (`docs/reports/W27-FIX-PASS.md`, deviations). "Append the cards to the board JSON" is
   therefore executed as: create `docs/board/W28-board.json` with the wave's cards, and publish
   a board artifact rendered from it, whose URL the JSON carries in `renders_to`. The per-card
   `.md` and the backlog statuses continue as before, so nothing the earlier waves rely on moves.
3. **"R-05" and "R-07" are `W27-R-05` and `W27-R-07`**, the wave 27 register's ids (the price
   display rule and the two design cards approved under delegation). No ruling in this repo is
   numbered `R-05` or `R-07` on its own; the reading is recorded under R-W28-01 below.

---

## R-W28-01 · Protection, self-merge, the two open reverts, W28-T

> R-W28-01 (branch protection on main, required check quality, no reviewers; self-merge on
> green quality check stays for wave 28 by strategy ruling; one card each to revert R-05 R-07
> stays open; W28-T not run)

**Four things in one ruling, each read separately.**

1. **Branch protection on `main`**: required status check `quality`, strict (the branch must
   be up to date with `main`), administrators not enforced, no required reviewers. Applied by
   card W28-10, which has no pull request by the dispatch's own words; its record is the block
   at the foot of this ruling.
2. **Self-merge on green `quality` stays for wave 28.** W27-R-01 covered wave 27 only and said
   so; this extends the same condition, and only that condition, to every wave 28 card. "On
   green quality check" is still the whole condition: the `quality` job has reported success on
   the pull request's head sha before the merge is called, never before. Every merge is a
   merge commit (docs/CLAUDE.md section 10), and every merge is followed by section 12.0
   against the merge sha. The addendum confirms it for the whole wave ("owner confirmed").
3. **The revert cards for W27-R-05 and W27-R-07 stay open.** Nothing this wave reverts the
   "de la N lei" price display or the floating header and sentence-case headings; both stand as
   shipped, and the one-card reverts remain available to the owner. W28-14 prices copertine
   "de la N" under W27-R-05 for that reason.
4. **W28-T is not run.** The typography card the wave 27 dispatch skipped (it needs a
   `build.js` split) stays skipped; no wave 28 card takes it.

### Record of W28-10, executed 2026-09-23 23:13 UTC, no pull request

```
gh api -X PUT repos/happygamer1919-tech/rc-website/branches/main/protection --input protection.json
  body: required_status_checks {strict: true, contexts: ["quality"]}, enforce_admins false,
        required_pull_request_reviews null, restrictions null
  exit 0
gh api repos/happygamer1919-tech/rc-website/branches/main/protection
  exit 0
  read back: contexts ["quality"], strict true, enforce_admins false, reviews null, restrictions null
```

Before the call the branch carried no protection at all (the GET answered `Branch not
protected`, 404, as it had at every preflight since wave 27). Nothing was deleted. The merge
protocol is unchanged in practice: the chain waits for `quality` on the head sha and then
merges; what changes is that the same condition is now also held by GitHub for anyone else who
merges, and a branch behind `main` is refused by the strict flag unless an administrator merges
it (the owner's own account, which this terminal uses, is one, and `enforce_admins` is false by
the ruling's own terms).

## R-W28-02 · The experience claim is fifteen years

> R-W28-02 experience claim: owner confirms 15 years. W28-11 unifies to "15+ ani de experienta"
> RO and the RU equivalent everywhere, including the team line (no "10+" anywhere). Acceptance
> changes: DOM grep for "10+" returns 0 across all pages, grep for "15+" returns at least 1 on
> home in both locales. The "15+" exclusion in the original W28-11 acceptance is dropped.

**This overturns the original W28-11 text ("unify experience to 10+ ani everywhere") before
the card ran, so the card runs on this ruling only.** The site carries the claim in three
shapes today, measured at boot: the hero and band stat `15+ ani de experiență` / `15+ лет
опыта` (`stats.1`), the trust item `Echipă cu 10+ ani experiență` / `Команда с опытом 10+ лет`
(`trust.items.3.title`, the "team line") and, in prose, "peste zece ani de experiență" / "более
десяти лет опыта" in two FAQ answers per locale. The figure is now fifteen in every shape: the
team line becomes the ruling's string with diacritics as the site writes them ("15+ ani de
experiență"; RU "15+ лет опыта"), and the two prose answers say fifteen. The RO string is
rendered with the site's diacritics; the owner typed it without, and the acceptance greps the
digits and the plus sign, which are the same either way.

## R-W28-03 · The Google reviews link is live now

> R-W28-03 Google reviews link: owner supplied https://share.google/t05qzuoiFA6LWKBF1 (public
> link, not a credential). Resolve it with curl -sIL and use the final URL if it lands on
> google.com/maps or a Google business page, else use the share link as given. In W28-11 the
> reviews spot becomes a live link now, text RO "Vezi recenziile pe Google", RU "Смотреть отзывы
> в Google", rel noopener, target _blank, placed under the testimonials. In W28-20 only the GSC
> meta tag remains blocked on owner; split the card, GBP part done in W28-11, GSC part stays
> blocked. Acceptance for the link: anchor present on / and /ru/, href resolves with HTTP 200
> via curl -sIL.

**Resolved at boot, 2026-09-23 23:10 UTC**: `curl -sIL` follows one 302 from `share.google` to
`https://www.google.com/share.google?q=t05qzuoiFA6LWKBF1` (200), and a full follow lands on a
`https://www.google.com/search?...&kgmid=/g/11xrn6_nk5&q=Rapid+Construct...` knowledge-panel
result, HTTP 200. That is neither `google.com/maps` nor a Google business page under the ruling's
test, so **the share link is used as given**. It is a public link and is printed here and in the
build; it is not a credential. The site already carried the mechanism (`GOOGLE_REVIEWS_URL` in
`build.js`, hidden while empty, and the strings `reviews.google` in both locales, which are the
ruling's exact texts); W28-11 makes the URL a committed constant rather than an environment
variable, so the link is in the built page on every runner, and moves the anchor under the
three testimonials. **W28-20 is split**: its Google Business Profile half is done by W28-11
under this ruling; its Search Console half stays blocked on the owner for the tag content, as
Q-W28-01.
