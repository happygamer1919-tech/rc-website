# RULING REGISTER W28-R · The wave 28 owner rulings, from 2026-09-23

**This file is the register of the `R-W28` ruling ids.** It continues
`docs/rulings/W27-R.md`, which holds `W27-R-01` to `W27-R-21` and is not renumbered. The ids
here are written the way the owner wrote them, `R-W28-01` onward, with the hyphens.

**Append-only under R-S.** Every ruling below is quoted exactly as the owner gave it in the
wave 28 dispatch of 2026-09-23 (evening, UTC) and its addendum of the same evening. The prose
under each quote is this terminal's reading and is open for ratification; the quote is not.

**The dispatch's own framing, recorded verbatim because it governs every card this wave:**

> EXECUTOR - boot per CLAUDE.md, work the board. Open wave 28 in rc-website. Main at bc3e3a9.
> Rulings first: write docs/rulings/W28-R.md with R-W28-01 (...). Append the cards below to the
> board JSON in docs/board/ with defaults, depends_on, machine-checkable acceptance, then work
> them in order, one card one PR, skip-not-halt, republish the board artifact after every board
> change. Plain hyphens only in commits, notes, code. Credential values never printed.

And from the addendum:

> Self-merge on green stays for the whole wave per R-W28-01, owner confirmed. Run through to
> W28-21 without waiting for owner input; anything blocked goes to the report, not a halt.

**Three premises in that framing were measured false at boot and are corrected here rather
than obeyed** (docs/CLAUDE.md, ground truth is the repo):

1. **`main` was at `5751d2c`, not `bc3e3a9`.** `bc3e3a9` is where the fix pass stood at 07:50
   UTC (#159); eight more cards merged during the day, #160 to #167 (W27-R-14 to W27-R-21,
   `docs/reports/W27-FIX-PASS.md`). Wave 28 opens on `5751d2c` (#167), the tip of `origin/main`
   at 23:08 UTC, with nothing open.
2. **No board JSON existed in `docs/board/`, and no board artifact.** The board of this repo
   has always been `docs/board/<card>.md` per card plus the statuses in `docs/BACKLOG.md`
   (`docs/reports/W27-FIX-PASS.md`, deviations). "Append the cards to the board JSON" is
   therefore executed as: create `docs/board/W28-board.json` with the wave's cards, and publish
   a board artifact rendered from it, whose URL the JSON carries in `renders_to`. The per-card
   `.md` and the backlog statuses continue as before, so nothing the earlier waves rely on moves.
3. **"R-05" and "R-07" are `W27-R-05` and `W27-R-07`**, the wave 27 register's ids (the price
   display rule and the two design cards approved under delegation). No ruling in this repo is
   numbered `R-05` or `R-07` on its own; the reading is recorded under R-W28-01 below.

---

## R-W28-01 · Protection, self-merge, the two open reverts, W28-T

> R-W28-01 (branch protection on main, required check quality, no reviewers; self-merge on
> green quality check stays for wave 28 by strategy ruling; one card each to revert R-05 R-07
> stays open; W28-T not run)

**Four things in one ruling, each read separately.**

1. **Branch protection on `main`**: required status check `quality`, strict (the branch must
   be up to date with `main`), administrators not enforced, no required reviewers. Applied by
   card W28-10, which has no pull request by the dispatch's own words; its record is the block
   at the foot of this ruling.
2. **Self-merge on green `quality` stays for wave 28.** W27-R-01 covered wave 27 only and said
   so; this extends the same condition, and only that condition, to every wave 28 card. "On
   green quality check" is still the whole condition: the `quality` job has reported success on
   the pull request's head sha before the merge is called, never before. Every merge is a
   merge commit (docs/CLAUDE.md section 10), and every merge is followed by section 12.0
   against the merge sha. The addendum confirms it for the whole wave ("owner confirmed").
3. **The revert cards for W27-R-05 and W27-R-07 stay open.** Nothing this wave reverts the
   "de la N lei" price display or the floating header and sentence-case headings; both stand as
   shipped, and the one-card reverts remain available to the owner. W28-14 prices copertine
   "de la N" under W27-R-05 for that reason.
4. **W28-T is not run.** The typography card the wave 27 dispatch skipped (it needs a
   `build.js` split) stays skipped; no wave 28 card takes it.

### Record of W28-10, executed 2026-09-23 23:13 UTC, no pull request

```
gh api -X PUT repos/happygamer1919-tech/rc-website/branches/main/protection --input protection.json
  body: required_status_checks {strict: true, contexts: ["quality"]}, enforce_admins false,
        required_pull_request_reviews null, restrictions null
  exit 0
gh api repos/happygamer1919-tech/rc-website/branches/main/protection
  exit 0
  read back: contexts ["quality"], strict true, enforce_admins false, reviews null, restrictions null
```

Before the call the branch carried no protection at all (the GET answered `Branch not
protected`, 404, as it had at every preflight since wave 27). Nothing was deleted. The merge
protocol is unchanged in practice: the chain waits for `quality` on the head sha and then
merges; what changes is that the same condition is now also held by GitHub for anyone else who
merges, and a branch behind `main` is refused by the strict flag unless an administrator merges
it (the owner's own account, which this terminal uses, is one, and `enforce_admins` is false by
the ruling's own terms).

## R-W28-02 · The experience claim is fifteen years

> R-W28-02 experience claim: owner confirms 15 years. W28-11 unifies to "15+ ani de experienta"
> RO and the RU equivalent everywhere, including the team line (no "10+" anywhere). Acceptance
> changes: DOM grep for "10+" returns 0 across all pages, grep for "15+" returns at least 1 on
> home in both locales. The "15+" exclusion in the original W28-11 acceptance is dropped.

**This overturns the original W28-11 text ("unify experience to 10+ ani everywhere") before
the card ran, so the card runs on this ruling only.** The site carries the claim in three
shapes today, measured at boot: the hero and band stat `15+ ani de experiență` / `15+ лет
опыта` (`stats.1`), the trust item `Echipă cu 10+ ani experiență` / `Команда с опытом 10+ лет`
(`trust.items.3.title`, the "team line") and, in prose, "peste zece ani de experiență" / "более
десяти лет опыта" in two FAQ answers per locale. The figure is now fifteen in every shape: the
team line becomes the ruling's string with diacritics as the site writes them ("15+ ani de
experiență"; RU "15+ лет опыта"), and the two prose answers say fifteen. The RO string is
rendered with the site's diacritics; the owner typed it without, and the acceptance greps the
digits and the plus sign, which are the same either way.

## R-W28-03 · The Google reviews link is live now

> R-W28-03 Google reviews link: owner supplied https://share.google/t05qzuoiFA6LWKBF1 (public
> link, not a credential). Resolve it with curl -sIL and use the final URL if it lands on
> google.com/maps or a Google business page, else use the share link as given. In W28-11 the
> reviews spot becomes a live link now, text RO "Vezi recenziile pe Google", RU "Смотреть отзывы
> в Google", rel noopener, target _blank, placed under the testimonials. In W28-20 only the GSC
> meta tag remains blocked on owner; split the card, GBP part done in W28-11, GSC part stays
> blocked. Acceptance for the link: anchor present on / and /ru/, href resolves with HTTP 200
> via curl -sIL.

**Resolved at boot, 2026-09-23 23:10 UTC**: `curl -sIL` follows one 302 from `share.google` to
`https://www.google.com/share.google?q=t05qzuoiFA6LWKBF1` (200), and a full follow lands on a
`https://www.google.com/search?...&kgmid=/g/11xrn6_nk5&q=Rapid+Construct...` knowledge-panel
result, HTTP 200. That is neither `google.com/maps` nor a Google business page under the ruling's
test, so **the share link is used as given**. It is a public link and is printed here and in the
build; it is not a credential. The site already carried the mechanism (`GOOGLE_REVIEWS_URL` in
`build.js`, hidden while empty, and the strings `reviews.google` in both locales, which are the
ruling's exact texts); W28-11 makes the URL a committed constant rather than an environment
variable, so the link is in the built page on every runner, and moves the anchor under the
three testimonials. **W28-20 is split**: its Google Business Profile half is done by W28-11
under this ruling; its Search Console half stays blocked on the owner for the tag content, as
Q-W28-01.

---

# Two chat instructions during the wave, 2026-09-24

**Appended under R-S and R-T on 2026-09-24 by card W28-15.** Nothing above this line moves. The
owner, following the wave in chat at about 00:45 UTC, gave two instructions in one message,
recorded verbatim and read here.

## R-W28-04 · The phone hero matches the desktop hero

> Make sure the phone hero matches the pc hero

**A ruling for the home page's first section.** What the desktop hero shows at 1440 (the eyebrow,
the heading, the sub-line, the three stat cards, the claim card with its button and the social
row, and the hero photograph panel) is what the phone hero shows at 390, in the same order,
stacked; nothing the desktop shows is hidden on the phone, and nothing the phone shows is absent
on the desktop. Measured before the card: the hero's children and their computed visibility at
both widths are recorded in card W28-22's entry, which does the work. Section 1 (nothing above
the fold animates) and the R-Y budgets apply as always.

## R-W28-05 · Services versus catalog, confirmed after the fact

> also not sure if it;s told in dispatch, but from Services/Acooprisuri the products with prices
> should be removed, only the projects and description about acoperisuri remains, Catalogu remains
> to hold the acoperisuri products as is, sme for garduri

**This is W28-13, already merged and verified live as #171 (`b843133`) an hour before the
message.** The dispatch's card said it ("Remove product grids, filter chips, counts and every
price from service pages ... Catalog keeps all 9 hub tiles ... and all 99 roofing cards plus
garduri cards unchanged"), and the live `/servicii/acoperisuri/` carries zero prices since
00:30 UTC. What the service page keeps, read from "only the projects and description": the
hero, the "ce include" text, the four roofing works (a description of the service, no price),
the projects, the FAQ and the quote form; it also keeps the four navigation tiles (to the tile
page, the Novatik page, the calculator and the form; no price) and gains six link cards into the
catalogue's groups. If "only" means the navigation tiles and the four works should go too, that
is one line each and is asked in the report, not assumed. "Same for garduri": `/servicii/garduri/`
carried no price before the wave and carries a link card to the fence models now; the Catalog's
Garduri tile opens the fence models with their prices, as is.

---

# The second dispatch of wave 28, 2026-09-24 (early morning, UTC): two cards from the client review

**Recorded verbatim, because two of its ids are already taken and are corrected here rather than
obeyed.** The dispatch:

> EXECUTOR - boot per CLAUDE.md, work the board. Wave 28 continues, two cards from client review,
> self-merge on green stays per R-W28-01. Record R-W28-04 (fatade images: no mark of any kind,
> maker's included) and R-W28-05 (service galleries labelled as examples, Portofoliu page RC-own
> only, stock never on it) in docs/rulings/W28-R.md. Plain hyphens only.

**Two premises corrected at boot:**

1. **`R-W28-04` and `R-W28-05` are taken** (the phone hero and the services-versus-catalog
   confirmation above, both merged and on the board). This register is append-only under R-S, so
   the two rulings the dispatch names are recorded as **R-W28-06** and **R-W28-07**, with the
   dispatch's wording quoted under each. Nothing above this line moves.
2. **`W28-22` is taken** (the phone hero, #174, verified). The two cards are **W28-23** (fatade
   images) and **W28-24** (service galleries); the report files keep the names the dispatch gave
   them (`docs/reports/W28-FATADE-IMAGES-2.md`, `docs/reports/W28-FINAL-RUN-2.md`).

## R-W28-06 · Fatade images: no mark of any kind, the maker's included

> Record R-W28-04 (fatade images: no mark of any kind, maker's included)

> Image sourcing rule for both cards: allowed sources are Pexels, Unsplash, Pixabay, Wikimedia
> Commons with CC0 or public domain, and Google Images only with usage-rights filter Creative
> Commons and the licence verified on the landing page. No attribution-required licences, no
> editorial-only, no watermark, no visible logo, brand, company name, vehicle livery or text
> overlay in the frame, no identifiable faces. Every image gets a row in docs/images/SOURCES.md:
> file, page, source URL, licence, subject. Convert to webp, max 1600px wide, lazy-loaded, alt
> text RO and RU. Extend scripts/image-sources test: every image under the fatade group and every
> gallery image has a manifest row whose licence is in the allowed set, and no fatade product
> image source host is fatade3d.md.

> W28-22 fatade product images: for every product in the fatade group (cards and detail pages, RO
> and RU) whose image shows the FATADE mark or logo, replace with a generic photo of the same
> product type: diffusion membrane roll, fibreglass reinforcement mesh roll, PVC corner bead with
> mesh, and so on per product. Specs, prices, names unchanged.

**What it overturns, named so the amendments are exact.** W27-R-13's reading that a maker's own
mark on a product picture is allowed (applied by W28-15 as "a small maker mark or the FATADE 3D
watermark is allowed", `docs/reports/W28-FATADE-IMAGES.md` line 10) is overturned for the fatade
group: no mark of any kind. W25-R14, which lifted the fatade3d.md image ban for product pictures
"watermark untouched", is reversed for the same group: no fatade product picture may come from
fatade3d.md. W25-R23 and W26-R8, which confined library pictures (Unsplash, Pexels) to secondary
images and gate 19's `LIBRARY_SLOTS`, are widened: a stock picture from the allowed set may stand
on a fatade-group product slot. W25-R4 ("never a near match") and W25-R17 ("a picture never fills
a different product") are amended for the same group and only there: **a generic photograph of the
product type may stand on every record of that type**, declared on each record after the first as
`reuse_of` the type's origin slot with `reuse_reason` naming this ruling, which keeps gate 19's
one-origin rule intact. W25-R2's closed list of stock sites gains Pixabay and Wikimedia Commons
(CC0 or public domain only) and Google Images under the owner's two conditions; attribution-only
licences (CC BY, CC BY-SA), editorial-only and anything with a watermark stay refused.

**Readings, open for ratification:**

- **"The fatade group" is the set of records whose data came from fatade3d.md**, `source.host ===
  'fatade3d.md'` in `content/catalog-products.json` (223 records across twelve category slugs;
  the catalogue has no category or field named fatade, and gate 32 already uses this definition).
  Their pictures split three ways today: **110 fetched from fatade3d.md** under W25-R14 (64 RED
  mouldings, 25 lamps, 21 material packshots; the FATADE print is burned into three of them,
  CAT-0221, CAT-0222 and CAT-0223, the very products the dispatch names, and eleven more show a
  maker's brand), **102 fetched from the manufacturer's own site** (Phomi 83, Caparol 15, others)
  under the W25-R1 packshot licence, and **11 owner-picked under W25-R20**. The test rule "no
  fatade product image source host is fatade3d.md" reaches the 110, and W28-23 replaces all 110
  with generic type photographs. **The 113 manufacturer and owner-picked packshots are left as
  they are and put to the owner as Q-W28-05**: a Caparol bucket's label is the product, not a
  third party's mark, and a generic bucket would make the card less true; but "the maker's
  included" can be read to reach them, so the owner decides, and the manifest rule below counts
  them as what they are instead of pretending they carry a stock licence.
- **"Cards and detail pages"**: a catalogue record has no detail page in this build (it renders
  as a card on its category page and, for a subcategory record, on the parent page, RO and RU).
  The card is the whole surface and is what the DOM check reads.
- **The manifest.** `docs/assets/PROVENANCE.md` stays the row every gate reads (R-W: file,
  source URL, licence, licence URL, date), and `docs/images/SOURCES.md` is the owner's manifest
  (file, page, source URL, licence, subject), written by the same intake step so the two cannot
  disagree; gate 32 holds every stock picture to a SOURCES row whose licence is in the allowed set.
- **The licence set, as strings gate 32 accepts**: `Pexels License`, `Unsplash License`, `Pixabay
  Content License`, `CC0 1.0` and `Public domain` (Wikimedia Commons), and `CC0 1.0 (Google
  Images, licence verified on the landing page)`.
- **WebP.** The repo has no WebP encoder and Q-W25-01 closed at "JPEG only, no new dependency";
  the pictures this ruling installs are encoded to WebP by the Chrome the gates already run
  (`scripts/webp-encode.js`, a canvas export, zero dependency), at most 1600px wide. That is not a
  new dependency and reopens nothing.
- **Alt text** is written per picture in both locales by the viewer that accepted it and lives in
  the ledger row (catalogue) or the gallery ledger entry (galleries).
- **The DOM check** "no fatade product image file name or alt contains fatade or fatade3d": file
  names are slot ids and contain neither today; 24 alts contain the Romanian word "fațadă" (a
  facade moulding is a facade moulding), which is not the supplier's name. The check reads
  `fatade3d`, `fatade 3d` and `FATADE` as a mark, not the common noun; recorded as a reading.

## R-W28-07 · Service galleries are labelled as examples; the Portofoliu section is RC-own only

> Record R-W28-05 (service galleries labelled as examples, Portofoliu page RC-own only, stock never
> on it)

> W28-23 service galleries: every service page RO and RU gets 10 additional images, fatade gets 15,
> Constructii industriale and Proiectare 3D get a gallery with 10 since they have none. Subjects
> match the service: metal tile and shingle roof installs, fence panel and gate installs, ETICS
> facade insulation and render work, canopy and carport structures, steel-frame warehouses and
> sandwich panel walls, architectural 3D renders for Proiectare 3D. Gallery heading on service
> pages becomes RO "Exemple de lucrari si solutii", RU "Примеры работ и решений". RC's own photos
> stay first in order, stock after. Portofoliu page untouched.

**What it amends.** W26-R14 made the galleries client-supplied-origin only ("reported, not
invented" for empty folders) and their ledger `content/galleries.json` a file "never typed": a
stock picture from R-W28-06's allowed set may now follow the owner's photographs in a service
gallery, appended after them in the ledger with `origin: "stock"`, its source, licence and alt in
both locales, and the two empty folders (industrial, proiectare-3d) gain a gallery of stock
pictures. The intake script keeps stock entries on a re-run instead of dropping them. W14-18's
proof-slot rule is untouched: the home page's Portofoliu section is built from
`content/projects.json` and reads no gallery, so it cannot receive a stock picture, and no
project cover, before-and-after pair, testimonial or team slot takes one.

**Readings, open for ratification:**

- **"Every service page"** is the nine service pages (`SERVICE_SLUGS`) plus the two product pages
  that carry a gallery of their own, copertine (2 photographs) and the fence gallery page
  (12), because the dispatch's subject list names canopies and fence panels. Eleven galleries:
  ten more pictures each, fifteen on fatade, ten on each of the two new ones; 115 stock pictures.
- **"Portofoliu page"** is the home page's `#portofoliu` section (there is no separate page).
- **The heading** "Exemple de lucrari si solutii" is rendered with the site's diacritics,
  "Exemple de lucrări și soluții", as R-W28-02 did for the experience claim; the Russian string
  is as given. It replaces the h2 of the projects-and-gallery section on the service pages
  through a key of its own, because the current h2 key is shared with the home page's Portofoliu
  section, which stays "Proiecte recente".
- **Lighthouse at or above 90 on every service page, both locales**: measured as the median of
  three desktop runs on every one of the eighteen service pages and recorded in
  `docs/reports/W28-FINAL-RUN-2.md`; gate 5 keeps its two home pages and its 95 floor, which
  section 4 of docs/CLAUDE.md owns.
- **Faces.** A photograph with an identifiable face is refused by the viewer; a worker seen from
  behind, in a helmet from a distance, or with the face turned away is not identifiable and may
  stand.

## R-W28-08 · The hub tiles leave the Acoperisuri and Garduri service pages; the compact catalogue links stay

> when I enter in https://rapidconstruct.md/servicii/acoperisuri/ there shouldn't pe the products
> displayed anymore because we moved succesfully all the products from Acoperisuri / Garduri to
> Catalog, and I asked yesterday that when I enter in Acoperisuri there shuold not be these tiles
> anymore, the page starts with "Servicii / Acoperișuri / Realizăm acoperișuri co..." what I like is
> this catalog you did, very compact and clear, we can keep it because they can access it from here
> directly to catalog, so I like it we leave it but the hub tiles have to go from here those remain
> in Catalog, same for Garduri - https://rapidconstruct.md/servicii/garduri/, the hub tiles and
> products remain in Catalog, the service description linked with projects pics is the only what
> remains in service page + the compact catalog already existent in Acoperisuri, that should be it

**Owner message of 2026-09-24, 17:11 UTC, with a thirty-minute limit.** This overturns W28-13's
reading (recorded under R-W28-05) that the hub tiles stayed on the service page as navigation.
Done as W28-25: the roofing hub ("Acoperișuri metalice", four tiles) leaves `/servicii/acoperisuri/`
and stays on `/catalog/materiale-acoperis/` where W28-13 put it; the fence hub (four tiles) leaves
`/servicii/garduri/` and renders on the fence models page `/servicii/modele-garduri/`, which is the
Catalog's Garduri entry, so no tile leaves the site. Both service pages open with their hero and
keep the service description, the projects and, on Acoperisuri, the compact six-card catalogue
block. Gates 25 and 26 read the hubs where they render now; the live markers follow (the two
service sets at zero tiles, a `product-fence` set at four for the fence models page); the budgets
of the four pages move (R-Y).
